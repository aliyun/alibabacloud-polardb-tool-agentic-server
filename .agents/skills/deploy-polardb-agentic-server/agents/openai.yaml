interface:
  display_name: "Deploy PAS"
  short_description: "Deploy PAS and deliver PolarRAG MCP safely"
  default_prompt: "Use $deploy-polardb-agentic-server to deploy PAS safely and, when explicitly requested, complete PolarRAG MCP onboarding and acceptance."
policy:
  allow_implicit_invocation: false
